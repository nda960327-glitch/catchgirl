DATABASE_URL="postgresql://user:password@host:5432/dbname"
AUTH_SECRET="catch-girl-dev-secret-change-me"

# 예약 시각은 서버의 로컬 시간대로 계산한다 (영업시간·슬롯·"지난 시간" 판정 모두).
# 기본값 Asia/Seoul 은 src/instrumentation.ts 에서 세운다 (Vercel 은 TZ 를 예약어로 막아
# 환경변수로 지정할 수 없다). 다른 지역 매장이면 이 값만 바꾸면 된다.
# APP_TZ="Asia/Seoul"
